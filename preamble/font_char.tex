% 字体字形设置

% 字体格式支持
\usepackage{fontspec}

% 中文字体支持，开启粗体和斜体
\usepackage[BoldFont, SlantFont]{xeCJK}

% 根据配置设置字体
\input{preamble/font_\targetfonts}

% 数学公式与数学符号
\usepackage{amsmath,amssymb}

% 让 _ 字符能直接使用
\usepackage{underscore}

 % 下划线，删除线
\usepackage{xeCJKfntef}
% 去掉删除线下划线之类的默认颜色
\xeCJKsetup{
    underdot/format=,
    underline/format=,
    underdblline/format=,
    underwave/format=,
    sout/format=,
    xout/format=,
}

% 导入特殊字符列表
\input{preamble/special_chars.tex}
